package com.example.godzilla.controller.gui;

import com.example.godzilla.bean.RegisterUserBean;
import com.example.godzilla.utils.NavigatorScene;
import javafx.fxml.FXML;

public class RegisterGui {

    /*
    @FXML
    private void register() {
        try {
            RegisterUserBean req = new RegisterUserBean();
            req.setUsername(usernameField.getText());
            req.setPassword(passwordField.getText());
            req.setEmail(emailField.getText());
            req.setRole(roleChoiceBox.getValue());

            // campi extra (se presenti nella UI)
            req.setCodiceFiscale(cfField.getText());

            authService.register(req);
            NavigatorScene.switchScene("login.fxml");

        } catch (Exception e) {
            errorLabel.setText(e.getMessage());
        }
    }

     */

}
